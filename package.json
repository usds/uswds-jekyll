{
  "private": true,
  "scripts": {
    "postinstall": "bundle",
    "start": "bundle exec jekyll serve",
    "build": "bundle exec jekyll build"
  },
  "dependencies": {
    "uswds": "^0.13.1"
  }
}
